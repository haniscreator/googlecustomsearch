<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>

    <php>
        <!-- Existing test-safe settings -->
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_DRIVER" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>

        <!-- Additional safe overrides to match .env.testing -->
        <env name="REDIS_CLIENT" value="predis"/>
        <env name="REDIS_HOST" value="127.0.0.1"/>
        <env name="REDIS_PASSWORD" value="null"/>
        <env name="REDIS_PORT" value="6379"/>
        <env name="REDIS_DB" value="0"/>
        <env name="REDIS_CACHE_DB" value="1"/>

        <!-- Search provider config (tests typically use Http::fake()) -->
        <env name="SEARCH_PROVIDER" value="google"/>
        <env name="SEARCH_API_ENDPOINT" value="https://www.googleapis.com/customsearch/v1"/>
        <env name="SEARCH_API_KEY" value="fake-key"/>
        <env name="GOOGLE_SEARCH_ENGINE_ID" value="fake-cx"/>
        <env name="SEARCH_API_QUERY_PARAM" value="q"/>
        <env name="SEARCH_API_TIMEOUT" value="1"/>

        <!-- Disable Sentry reporting during tests -->
        <env name="SENTRY_LARAVEL_DSN" value=""/>
        <env name="SENTRY_ENVIRONMENT" value="testing"/>
        <env name="SENTRY_TRACES_SAMPLE_RATE" value="0.0"/>
        <env name="SENTRY_PROFILES_SAMPLE_RATE" value="0.0"/>
    </php>
</phpunit>
